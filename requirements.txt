pyyaml>=6.0
sounddevice>=0.4.6
vosk>=0.3.38
pynput>=1.7.6

# Web app: run control + command editor (app.sh)
flask>=2.3
ruamel.yaml>=0.18

# Optional: only needed if you set `engine: rhino` in config.yaml.
# Requires a free Picovoice account + AccessKey (https://console.picovoice.ai).
# pvrhino>=3.0.0
